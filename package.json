{
  "name": "mmcss",
  "version": "0.2.4",
  "description": "A Minimal Modern CSS Framework.",
  "main": "dist/mm.css",
  "dependencies": {},
  "devDependencies": {
    "mmpilot": "^0.3.1",
    "npm-run-all": "^3.1.0",
    "shx": "^0.1.4",
    "stylelint": "^7.3.1"
  },
  "keywords": [
    "css",
    "css3",
    "front-end",
    "framework",
    "html",
    "html5",
    "responsive",
    "mobile",
    "scss"
  ],
  "scripts": {
    "clean": "mmpilot clean",
    "build": "mmpilot build",
    "serve": "mmpilot serve",
    "dev-build": "mmpilot build -d",
    "dev-watch": "mmpilot watch -d",
    "dev": "npm-run-all clean dev-build --parallel dev-watch serve",
    "deploy": "npm-run-all clean build && mmpilot deploy",
    "dev-dist": "npm-run-all clean dev-build && shx cp public/styles/mm.css dist",
    "prod-dist": "npm-run-all clean build && shx cp public/styles/mm.css dist/mm.min.css",
    "lint": "stylelint \"src/**/*.scss\" --syntax scss",
    "dist": "npm-run-all lint dev-dist prod-dist && git add dist/*.css && git commit -m \"Updating dist\" && git push -u origin master",
    "dist-patch": "npm run dist && npm version patch -m \"Version %s\" && git push && git push --tags && npm publish"
  },
  "engines": {
    "node": ">=4.2.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kunruch/mmcss.git"
  },
  "author": "Kanishk Kunal",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/kunruch/mmcss/issues"
  },
  "homepage": "https://mmcss.kunruchcreations.com/"
}
