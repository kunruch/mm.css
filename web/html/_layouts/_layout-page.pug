extends ./_layout-default.pug

block content
  article
    .py-4.text-center
      .container-readable
        h1.mt-0 #{page.title}
        p.text-muted.text-large #{page.description}
    .py!= page.content